== README
This looks like a rails project, but the only thing of relevance is the rake task named test

It creates a rails migration and runs it and copies a csv into it.

to use type rake csv_to_pg filepath=YOUR_FULL_FILEPATH modelname=NAME_OF_MODEL_TO_BE_GENERATED
Look inside it and grab parts at will
